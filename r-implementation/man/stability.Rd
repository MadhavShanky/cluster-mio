% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scs.R
\name{stability}
\alias{stability}
\alias{stability.scs}
\title{Stability selection over subsamples (Meinshausen-Buhlmann / CPSS)}
\usage{
stability(object, ...)

\method{stability}{scs}(
  object,
  B = 100L,
  frac = 0.5,
  pi_thr = 0.6,
  seed = 1L,
  scheme = c("obs", "cluster_strat"),
  pairing = c("independent", "complementary"),
  n_restart = 2L,
  ...
)
}
\arguments{
\item{object}{an \code{"scs"} fit.}

\item{...}{passed to methods.}

\item{B}{integer number of subsample fits (for \code{pairing = "complementary"}
this is \code{2 * #pairs} and must be a positive even number).}

\item{frac}{subsample fraction (forced to 0.5 under complementary pairing).}

\item{pi_thr}{selection-frequency threshold for flagging.}

\item{seed}{integer RNG seed.}

\item{scheme}{\code{"obs"} (observation-level subsampling, default) or
\code{"cluster_strat"} (within-cluster subsampling so every candidate cluster
is eligible in every subsample).}

\item{pairing}{\code{"independent"} (default; B independent half-subsamples) or
\code{"complementary"} (Shah-Samworth complementary pairs: equal floor(n/2)
halves; the CPSS sampling structure that makes the reference a genuine
false-inclusion bound).}

\item{n_restart}{integer local-search restarts inside each subsample fit.}
}
\value{
An object of class \code{"scs_stability"}.
}
\description{
Subsamples the observations \code{B} times, refits the selector, and records the
per-cluster selection frequency, the set flagged at threshold \code{pi_thr}, and
the implied bound on the expected number of false flags.
}
