% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scs.R
\name{scs_path}
\alias{scs_path}
\title{Solve the symmetric L0 problem along the budget path}
\usage{
scs_path(
  X,
  y,
  cluster,
  lambda_max = NULL,
  mu = NULL,
  eps = 1e-04,
  refit = TRUE,
  intercept = TRUE,
  n_restart = 4L,
  seed = 1L
)
}
\arguments{
\item{X, y, cluster}{as in \code{scs_fit}.}

\item{lambda_max}{maximum budget (default \code{min(K-1, max(2, ceiling(K/2)))}).}

\item{mu, eps, refit, intercept, n_restart, seed}{as in \code{scs_fit} (mu fixed once
from the full design so the ridge is constant along the path).}
}
\value{
An object of class \code{"scs_path"} with the per-budget objective and the
  \code{K x (lambda_max+1)} membership matrix.
}
\description{
Fits \code{scs_fit} at every budget \code{lambda = 0, 1, ..., lambda_max} with a
single fixed ridge \code{mu} (so the objectives are comparable across budgets).
Shared engine for \code{audit_budget} and \code{cluster_ranking}.
}
